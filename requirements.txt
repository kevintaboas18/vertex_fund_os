# M-13: rangos con techo. Sin techo, un `pip install` futuro puede traer una
# major con cambios incompatibles y romper el scoring en silencio.
# Los techos son de MAJOR: 2006 tests pasan con pandas 3.0.3 y numpy 2.5.1,
# asi que fijar <3 habria forzado un downgrade sin motivo.
fastapi>=0.115,<1
uvicorn>=0.30,<1
numpy>=1.26,<3
requests>=2.31,<3
pydantic>=2.7,<3
google-genai>=1.0,<3
python-dotenv>=1.0,<2
scipy>=1.13,<2
httpx>=0.27,<1
typer>=0.12,<1
pandas>=2.2,<4
anthropic>=0.40
# Graficos del reporte (report/charts.py). Import diferido, headless (Agg).
matplotlib>=3.8
# C-03: cifrado en reposo del access_token de Plaid (opcional, via VERTEX_DB_KEY)
cryptography>=42
# Solo para las CADENAS DE OPCIONES de Proyecciones. El analisis de acciones
# no lo toca: sus fuentes son FMP, FinnHub, FRED y EDGAR.
yfinance>=0.2,<2
